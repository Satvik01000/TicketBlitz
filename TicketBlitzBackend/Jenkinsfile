pipeline {
    agent any

    stages {
        stage('Build with Maven') {
            steps {
                dir('TicketBlitzBackend')
                sh 'mvn clean package -DskipTests'
            }
            post {
                success {
                    echo 'Maven build successful'
                }
                failure {
                    echo 'Maven build failed'
                }
            }
        }
    }
}
